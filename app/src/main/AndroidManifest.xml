<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.starwarsapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.StarWarsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ShowFilmFragment"></activity>
        <activity android:name=".ui.ShowSpecieFragment" />
        <activity android:name=".ui.ShowVehicleFragment" />
        <activity android:name=".ui.ShowStarshipFragment" />
        <activity android:name=".ui.ShowPlanetFragment" />
        <activity android:name=".ui.ShowPeopleFragment" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>