<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.starwarsapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.StarWarsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ShowFilmActivity"></activity>
        <activity android:name=".ui.ShowSpecieActivity" />
        <activity android:name=".ui.ShowVehicleActivity" />
        <activity android:name=".ui.ShowStarshipActivity" />
        <activity android:name=".ui.ShowPlanetActivity" />
        <activity android:name=".ui.ShowPeopleActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>