<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.starwarsapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.StarWarsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.fragment.ShowFilmFragment" />
        <activity android:name=".ui.fragment.ShowSpecieFragment" />
        <activity android:name=".ui.fragment.ShowVehicleFragment" />
        <activity android:name=".ui.fragment.ShowStarshipFragment" />
        <activity android:name=".ui.fragment.ShowPlanetFragment" />
        <activity android:name=".ui.fragment.ShowPeopleFragment" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>